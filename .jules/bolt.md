## 2024-05-18 - [Add `.with_capacity()` for Vec and Hashmap initializations] **Learning:** [Pre-allocating capacities for vectors and hash maps where bounds are known avoids multiple re-allocations on hot paths.] **Action:** [Identify collections initialized with `Vec::new()` or `HashMap::new()` in critical loops or parse phases where target size is already known, and replace them with `with_capacity()`]

## 2024-11-23 - [Integrate Rust Hexagonal Layering & DDD Fixtures] **Learning:** [vord classifies hexagonal layers and detects DDD anti-patterns structurally using AST traversal. Since there were no Rust hexagonal/DDD files under 'fixtures/hexagon/', adding 'fixtures/hexagon/src/domain/order.rs' and 'infrastructure/postgres_orders.rs' ensures the engine end-to-end correctly handles and detects Rust-specific architectural and tactical DDD rules.] **Action:** [When expanding support or adding structural rules, always write multi-language test fixtures under 'fixtures/' representing both the safe and violating configurations to keep coverage high and force edge cases early.]
