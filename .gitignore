/target
/mutants.out
**/*.rs.bk
.env
.vord-cache.json
.vord-baseline.json
.vord-circuit-breaker.json
.vord-approvals.json
.vord-loop-guard.json
.vord-audit.jsonl
.vord-provenance.json
.vord-triage.json
.vord/
.freebuff/

# `.claude/settings.json` is deliberately NOT ignored: `vord hook install`
# writes it so every teammate and CI run is gated by the same policy, which
# only works if it is committed. Only the per-developer override is local.
.claude/settings.local.json

# The npm wrapper downloads this at install time.
npm/bin/vord
npm/bin/vord.exe

# Benchmark runner targets & reports
.benchmark-targets/
.benchmark-results/

/.benchmark-corpora
.DS_Store
